[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
markers =
    slow: integration tests needing an external emulator
        (e.g. DynamoDB Local); skipped when it is unavailable.
