# XIN-36: all dependencies pinned with ~= (compatible release) so installs are
# reproducible. Removed 2026-09-11: uvicorn and asyncpg (zero imports repo-wide).
fastapi~=0.100.0
sqlalchemy~=2.0.0
aiosqlite~=0.19.0
pydantic~=2.0.0
pytest~=7.4.0
pytest-asyncio~=0.21.0
feedparser~=6.0.0
httpx~=0.27.0
python-dateutil~=2.9.0
pyyaml~=6.0.0
alembic~=1.13.0
aioboto3==15.5.0
boto3==1.40.61
botocore==1.40.61
moto[dynamodb]==5.2.3  # test-only: DynamoDB mock for unit tests; do not ship in production installs

# Optional: only needed for GCPCloudTasksDriver (backend/ingestion/task_queue/gcp.py).
# Declared separately so base installs stay cloud-free:
# google-cloud-tasks~=2.0.0
